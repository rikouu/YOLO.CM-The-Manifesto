# ============================================
# YOLO.CM 后端环境变量配置
# ============================================
# 复制此文件为 .env.local 并填入实际值
# cp .env.example .env.local
# ============================================

# --------------------------------------------
# Gemini API 配置
# --------------------------------------------
# Google Gemini API 密钥
# 获取地址：https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Gemini API 代理地址（可选）
# 如果 Gemini API 在你的地区不可用，需要配置代理
# 可以使用 Cloudflare Worker 或自建代理服务器
# 格式：http://your-proxy-server:port 或 https://your-worker.workers.dev
# GEMINI_PROXY_URL=http://your-proxy-server:8787

# --------------------------------------------
# 服务器配置
# --------------------------------------------
# 后端服务监听端口
# 默认值：4002
PORT=4002

# --------------------------------------------
# JWT 认证配置
# --------------------------------------------
# JWT 签名密钥（生产环境必须修改！）
# 建议使用随机生成的 32 字节十六进制字符串
# 生成命令：node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-random-secret-key-change-in-production

# --------------------------------------------
# CORS 配置（可选）
# --------------------------------------------
# 允许的前端域名，用于 CORS 跨域设置
# 本地开发：http://localhost:3001
# 生产环境：https://yolo.cm
# FRONTEND_URL=http://localhost:3001
