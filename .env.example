# ============================================
# YOLO.CM 前端环境变量配置
# ============================================
# 复制此文件为 .env.local 并填入实际值
# cp .env.example .env.local
# ============================================

# --------------------------------------------
# API 地址配置
# --------------------------------------------
# 本地开发模式：留空，前端会使用 Vite proxy 代理 API 请求
# 生产环境模式：设置为实际后端地址，如 https://yolo.cm
#
# 工作原理：
# - 为空时：/api/* 请求通过 Vite 代理转发到 VITE_PROXY_TARGET
# - 有值时：/api/* 请求直接发送到该地址（禁用代理）
VITE_API_URL=

# --------------------------------------------
# 代理目标（仅本地开发时生效）
# --------------------------------------------
# 当 VITE_API_URL 为空时，Vite 开发服务器会将 /api 和 /uploads
# 请求代理到此地址
# 默认值：http://localhost:4002
VITE_PROXY_TARGET=http://localhost:4002

# --------------------------------------------
# 静态资源地址
# --------------------------------------------
# 用于加载用户头像、挑战图片等静态资源
# 本地开发时，可以指向生产服务器以获取真实图片
# 生产环境通常与 VITE_API_URL 相同或留空
VITE_ASSET_URL=https://yolo.cm

# --------------------------------------------
# 前端开发服务器端口（可选）
# --------------------------------------------
# Vite 开发服务器监听的端口
# 默认值：3001
VITE_DEV_PORT=3001
